import React from "react";
import { createRoot } from "react-dom/client";

function App() {
  return <h2>Hello from Cheapest Flight Agent</h2>;
}

createRoot(document.getElementById("root")!).render(<App />);
